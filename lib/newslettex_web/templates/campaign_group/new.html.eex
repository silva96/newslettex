<h1>New Campaign group</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.campaign_group_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.campaign_group_path(@conn, :index) %></span>
