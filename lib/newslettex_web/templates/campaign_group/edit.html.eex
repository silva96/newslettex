<h1>Edit Campaign group</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.campaign_group_path(@conn, :update, @campaign_group)) %>

<span><%= link "Back", to: Routes.campaign_group_path(@conn, :index) %></span>
